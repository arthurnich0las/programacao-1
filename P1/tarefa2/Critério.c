#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<math.h>

int main()
{
    int gen, age;
    scanf("%d\n%d", &gen, &age);
    if (gen == 0 && age == 1) {printf("1");}
    else {printf("0");}
}